DEEPSEEK_API_KEY=your_deepseek_api_key_here
# 用于：实习对话、智能日历 等需调用大模型的能力

# 搜狗微信搜索（weixin.sogou.com）。不配则匿名访问，易被验证码/限速；从浏览器登录搜狗后复制 Cookie 整串填入。
# 会过期，需定期更新
# SOGOU_COOKIE=

# 用户注册登录（JWT）。生产环境务必改为长随机串
JWT_SECRET=change-me-to-a-long-random-string
# 生产环境 HTTPS 时设为 1，使 Cookie 带 Secure；本地 http 开发可设为 0 或不设
AUTH_COOKIE_SECURE=1

# 生产环境建议设为 production，此时必须同时设置下面的 ALLOWED_ORIGINS
# APP_ENV=production

# 允许的前端域名（逗号分隔）。不设则仅允许本地 http://localhost:3000 等（见 main.py 日志）。
# 部署后填入 Vercel / 自定义域名 的前端 URL
# ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-custom-domain.com

# 可选：自定义 SQLite 路径（默认 backend/data/app.db）
# DATABASE_URL=sqlite:///./data/app.db

# ── Hugging Face（sentence-transformers 首次会从 Hub 下载模型）──
# 国内访问 huggingface.co 易超时：任选一种方式
# USE_HF_MIRROR=1
# 或显式指定镜像端点：
# HF_ENDPOINT=https://hf-mirror.com
# 仅想起服务、稍后再下载模型时：
SKIP_EMBEDDER_WARMUP=1
